<style type="text/css">
  td, th {
    padding: 5px;
  }
</style>

<h1>Listing All Nodes</h1>
<p>
  <% bob = @nodes.find(1) %>
  <%= link_to "Create a new node", { :controller => "nodes", :action => "new", :parent => bob } %>
</p>

<table>
  <tr>
    <th>Select Options:</th>
    <th>Content</th>
    <th>Position</th>
  </tr>
  <% @nodes.each do |f| %>
    <tr>
      <td><%= f.title %></td>
      <td><%= f.content %></td>
      <td><%= f.position %></td>
      <td><%= link_to "Edit", { :controller => "nodes", :action => "edit", :id => f, :parent => f.id } %></td>
      <td><%= link_to "Show", node_path(f) %></td>
      <td><%= link_to "Destroy", node_path(f), method: :delete, data: {confirm: "Are you sure?"}%></td>
    </tr>
  <% end %>
</table>
